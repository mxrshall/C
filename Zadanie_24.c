#include <stdio.h>

int main(void){

int r, s, i;

for(r=0;r<4;i++){
    printf("Zadaj prvok do stpca matice [%d] [%d] [%d] [%d].", s, s, s, s);
    scanf("%d %d %d %d", &s, &s, &s, &s);
}

    return 0;
}